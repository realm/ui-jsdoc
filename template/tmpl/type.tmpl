<?js
	var data = obj;
    var self = this;
    data.forEach(function(name, i) { ?>
        <span class="param-type">
            <code><?js= self.typelink(name) ?></code>
        </span>
        <?js if (i < data.length-1) { ?>or<?js } ?>
<?js }); ?>