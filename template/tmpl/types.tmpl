<?js
var data = obj;
var self = this;
var types = data ? data.names || [] : [];
if (types.length) { ?>
<div class="detail-content">
    <?js
    var t = types.map(function(type) {
        return '<code class="text-nowrap">' + self.typelink(type) + '</code>';
    }).join(' or '); ?>

    <h5><b>Type</b>:</h5> <?js= t ?>
</div>
<?js } ?>
